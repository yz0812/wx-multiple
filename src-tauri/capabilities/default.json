{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability for the Weixin multi launcher.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
